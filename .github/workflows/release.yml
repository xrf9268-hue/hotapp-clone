name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing v* tag to publish — dispatch FROM that tag. Leave empty to rehearse; a dry run must not set this."
        required: false
        default: ""
        type: string
      dry_run:
        description: Build and validate everything, publish nothing
        required: false
        default: true
        type: boolean

permissions:
  contents: write

# Serialize ALL release runs: two tags racing on the R2 appcast upload must never interleave
# (spec §3). GitHub holds 1 running + 1 pending; a 3rd run cancels the pending one — re-run it.
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  release-readiness:
    name: Check release prerequisites
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      release_ready: ${{ steps.check.outputs.release_ready }}
      missing_secrets: ${{ steps.check.outputs.missing_secrets }}
      signing_mode: ${{ steps.check.outputs.signing_mode }}
      release_ref: ${{ steps.resolve.outputs.release_ref }}
      release_checkout_ref: ${{ steps.resolve.outputs.release_checkout_ref }}
    steps:
      - name: Resolve requested release ref
        id: resolve
        env:
          MANUAL_RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ] && [ -n "${MANUAL_RELEASE_TAG}" ]; then
            RELEASE_REF="${MANUAL_RELEASE_TAG}"
            # Fully qualified so a branch sharing the tag's name cannot win the
            # checkout resolution and have the gate scan the wrong revision.
            RELEASE_CHECKOUT_REF="refs/tags/${MANUAL_RELEASE_TAG}"
          else
            RELEASE_REF="${GITHUB_REF_NAME}"
            # The commit, not the branch. A branch ref is mutable: the
            # dependency job resolves it when it starts, while the release job
            # builds the immutable SHA captured when the run was created, so a
            # push in between would validate a revision that is never built.
            RELEASE_CHECKOUT_REF="${GITHUB_SHA}"
          fi

          echo "release_ref=$RELEASE_REF" >> "$GITHUB_OUTPUT"
          echo "release_checkout_ref=$RELEASE_CHECKOUT_REF" >> "$GITHUB_OUTPUT"

      - name: Check release secrets
        id: check
        env:
          DEVELOPER_ID_APP_CERT_BASE64: ${{ secrets.DEVELOPER_ID_APP_CERT_BASE64 }}
          DEVELOPER_ID_APP_CERT_PASSWORD: ${{ secrets.DEVELOPER_ID_APP_CERT_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
          DEVELOPER_ID_APP_SIGNING_IDENTITY: ${{ secrets.DEVELOPER_ID_APP_SIGNING_IDENTITY }}
          NOTARYTOOL_KEY: ${{ secrets.NOTARYTOOL_KEY }}
          NOTARYTOOL_KEY_ID: ${{ secrets.NOTARYTOOL_KEY_ID }}
          NOTARYTOOL_ISSUER: ${{ secrets.NOTARYTOOL_ISSUER }}
          SPARKLE_PUBLIC_ED_KEY: ${{ secrets.SPARKLE_PUBLIC_ED_KEY }}
          SPARKLE_PRIVATE_ED_KEY: ${{ secrets.SPARKLE_PRIVATE_ED_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_BASE_URL: ${{ secrets.R2_PUBLIC_BASE_URL }}
        run: |
          # Core secrets (Sparkle feed signing + R2 distribution) are always required.
          # Apple secrets are optional AS A COMPLETE SET (issue #283): all present =
          # Developer ID signing + notarization; all absent = ad-hoc interim mode;
          # a partial set is a misconfiguration and fails instead of degrading silently.
          core_missing=()
          for secret_name in \
            SPARKLE_PUBLIC_ED_KEY \
            SPARKLE_PRIVATE_ED_KEY \
            R2_ACCOUNT_ID \
            R2_ACCESS_KEY_ID \
            R2_SECRET_ACCESS_KEY \
            R2_BUCKET \
            R2_PUBLIC_BASE_URL
          do
            if [ -z "${!secret_name:-}" ]; then
              core_missing+=("$secret_name")
            fi
          done

          apple_missing=()
          for secret_name in \
            DEVELOPER_ID_APP_CERT_BASE64 \
            DEVELOPER_ID_APP_CERT_PASSWORD \
            KEYCHAIN_PASSWORD \
            DEVELOPER_ID_APP_SIGNING_IDENTITY \
            NOTARYTOOL_KEY \
            NOTARYTOOL_KEY_ID \
            NOTARYTOOL_ISSUER
          do
            if [ -z "${!secret_name:-}" ]; then
              apple_missing+=("$secret_name")
            fi
          done

          if [ "${#core_missing[@]}" -gt 0 ]; then
            missing_csv="$(IFS=,; echo "${core_missing[*]}")"
            echo "release_ready=false" >> "$GITHUB_OUTPUT"
            echo "missing_secrets=$missing_csv" >> "$GITHUB_OUTPUT"
            echo "signing_mode=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "${#apple_missing[@]}" -eq 0 ]; then
            SIGNING_MODE=developer-id
          elif [ "${#apple_missing[@]}" -eq 7 ]; then
            SIGNING_MODE=adhoc
            echo "::notice title=Ad-hoc release mode::No Apple Developer secrets configured — releasing ad-hoc signed, unnotarized artifacts (issue #283)."
          else
            apple_csv="$(IFS=,; echo "${apple_missing[*]}")"
            echo "::error title=Partial Apple secrets::Configure all Apple secrets or none. Missing: $apple_csv" >&2
            exit 1
          fi

          echo "release_ready=true" >> "$GITHUB_OUTPUT"
          echo "missing_secrets=" >> "$GITHUB_OUTPUT"
          echo "signing_mode=$SIGNING_MODE" >> "$GITHUB_OUTPUT"

  release-unavailable:
    name: Skip release without signing credentials
    needs: release-readiness
    if: ${{ needs.release-readiness.outputs.release_ready != 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Summarize skipped release
        env:
          RELEASE_REF: ${{ needs.release-readiness.outputs.release_ref }}
          MISSING_SECRETS: ${{ needs.release-readiness.outputs.missing_secrets }}
        run: |
          echo "::notice title=Release skipped::Missing required release secrets: $MISSING_SECRETS"

          {
            echo "## Release skipped"
            echo
            echo "Wink's release pipeline always requires Sparkle signing keys and Cloudflare R2 credentials (Apple Developer secrets are optional — without them releases ship ad-hoc signed, see issue #283)."
            echo "The required core secrets below are not configured, so no release or Sparkle update feed was published."
            echo
            echo "Missing secrets:"

            IFS=',' read -r -a missing <<< "$MISSING_SECRETS"
            for secret_name in "${missing[@]}"; do
              echo "- \`$secret_name\`"
            done

            echo
            echo "For an internal test build, run the \`Internal Package\` workflow with \`package_ref=$RELEASE_REF\`."
          } >> "$GITHUB_STEP_SUMMARY"

  # A public release depends on a FRESH dependency scan (issue #441). The scan a
  # pull request saw weeks ago says nothing about advisories published since,
  # and this is fail-closed on purpose: no scan, no release.
  #
  # `security-events: write` is granted here because the called workflow declares
  # it for its code-scanning upload; that upload is skipped on this path (it runs
  # only on schedule/manual), so the release job's own token is unaffected.
  dependency-scan:
    name: Scan dependencies before publishing
    needs: release-readiness
    if: ${{ needs.release-readiness.outputs.release_ready == 'true' }}
    permissions:
      contents: read
      security-events: write
    uses: ./.github/workflows/osv-scan.yml
    # Scan the ref that will actually be built and published, fully qualified.
    # A manual dispatch from a branch with `release_tag` set builds
    # `refs/tags/<tag>`, so without this the gate would clear the branch; and an
    # unqualified `vX.Y.Z` could resolve to a branch of the same name.
    with:
      ref: ${{ needs.release-readiness.outputs.release_checkout_ref }}

  release:
    name: Build, Release DMG, and Publish Sparkle Feed
    needs: [release-readiness, dependency-scan]
    if: ${{ needs.release-readiness.outputs.release_ready == 'true' }}
    runs-on: macos-15
    timeout-minutes: 45
    # Scoped to this job only (issue #443). `id-token` mints the Sigstore OIDC
    # token and `attestations` persists the result; no other job in this
    # workflow gets either. `contents: write` stays because this job creates the
    # GitHub Release. Declaring permissions here replaces the workflow-level
    # default for this job, so the full set must be listed.
    permissions:
      contents: write
      id-token: write
      attestations: write
    env:
      SIGNING_MODE: ${{ needs.release-readiness.outputs.signing_mode }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set temporary paths
        run: |
          echo "KEYCHAIN_PATH=$RUNNER_TEMP/wink-release.keychain-db" >> "$GITHUB_ENV"
          echo "CERT_PATH=$RUNNER_TEMP/wink-developer-id.p12" >> "$GITHUB_ENV"
          echo "NOTARY_KEY_PATH=$RUNNER_TEMP/AuthKey.p8" >> "$GITHUB_ENV"

      - name: Checkout manual release tag
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.release_tag != '' }}
        env:
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          case "$RELEASE_TAG" in
            v*) ;;
            *)
              echo "Error: workflow_dispatch release_tag must start with 'v', got '$RELEASE_TAG'" >&2
              exit 1
              ;;
          esac

          git fetch --force --tags origin
          TAG_SHA="$(git rev-parse "refs/tags/$RELEASE_TAG^{commit}")"

          # `--ref <tag>` fixes GITHUB_SHA when the run is created, but the tag
          # itself can be force-moved before this fetch. Building the tag's new
          # target while the attestation certificate still names the original
          # GITHUB_SHA would publish provenance for code that did not produce
          # the artifact — so bind the build to the commit provenance will name.
          if [ "$TAG_SHA" != "$GITHUB_SHA" ]; then
            echo "::error title=Tag moved after dispatch::refs/tags/$RELEASE_TAG now resolves to $TAG_SHA, but this run was created at $GITHUB_SHA and its attestation would name that commit. Re-dispatch so the two agree." >&2
            exit 1
          fi

          git checkout --force "$GITHUB_SHA"

      - name: Capture toolchain cache key
        run: echo "TOOLCHAIN_KEY=$(swift --version 2>/dev/null | head -1 | shasum -a 256 | cut -c1-12)" >> "$GITHUB_ENV"

      - name: Cache SwiftPM build
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .build
          key: spm-${{ runner.os }}-${{ env.TOOLCHAIN_KEY }}-${{ hashFiles('Package.resolved') }}
          restore-keys: spm-${{ runner.os }}-${{ env.TOOLCHAIN_KEY }}-

      - name: Resolve release metadata
        env:
          MANUAL_RELEASE_TAG: ${{ inputs.release_tag }}
          DRY_RUN_INPUT: ${{ inputs.dry_run }}
          R2_PUBLIC_BASE_URL_RAW: ${{ secrets.R2_PUBLIC_BASE_URL }}
        run: |
          VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' Sources/Wink/Resources/Info.plist)"

          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            if [ -z "${MANUAL_RELEASE_TAG}" ]; then
              # Ref rehearsal: no tag to operate on, publishing is impossible by construction.
              RELEASE_TAG=""
              DRY_RUN=true
            else
              RELEASE_TAG="${MANUAL_RELEASE_TAG}"
              DRY_RUN="${DRY_RUN_INPUT:-true}"
            fi
          else
            RELEASE_TAG="${GITHUB_REF_NAME}"
            DRY_RUN=false
          fi

          if [ -n "$RELEASE_TAG" ]; then
            case "$RELEASE_TAG" in
              v*) ;;
              *)
                echo "Error: release workflow requires a v* tag, got '$RELEASE_TAG'" >&2
                exit 1
                ;;
            esac

            if [ "v${VERSION}" != "$RELEASE_TAG" ]; then
              echo "Error: tag '$RELEASE_TAG' does not match Info.plist version 'v${VERSION}'" >&2
              exit 1
            fi
          fi

          R2_PUBLIC_BASE_URL="$(printf '%s' "$R2_PUBLIC_BASE_URL_RAW" | sed 's#/*$#/#')"

          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "RELEASE_TAG=$RELEASE_TAG" >> "$GITHUB_ENV"
          echo "RELEASE_TAG_OR_PLANNED=${RELEASE_TAG:-v$VERSION}" >> "$GITHUB_ENV"
          echo "DRY_RUN=$DRY_RUN" >> "$GITHUB_ENV"
          echo "DMG_PATH=build/Wink-${VERSION}.dmg" >> "$GITHUB_ENV"
          echo "ZIP_PATH=build/Wink-${VERSION}.zip" >> "$GITHUB_ENV"
          echo "APPCAST_PATH=build/appcast.xml" >> "$GITHUB_ENV"
          echo "APP_NOTARIZATION_ZIP_PATH=$RUNNER_TEMP/Wink-${VERSION}-notarization.zip" >> "$GITHUB_ENV"
          echo "R2_PUBLIC_BASE_URL=$R2_PUBLIC_BASE_URL" >> "$GITHUB_ENV"
          echo "SPARKLE_FEED_URL=${R2_PUBLIC_BASE_URL}appcast.xml" >> "$GITHUB_ENV"

      # Sigstore's certificate records `github.ref` — the ref that TRIGGERED the
      # run — not whatever a later step checked out. A manual dispatch from a
      # branch with `release_tag` set builds the tag but would mint provenance
      # naming the branch, so `gh attestation verify --source-ref refs/tags/vX.Y.Z`
      # would reject the very release it was published for. Rehearsals are
      # unaffected: they are expected to name a branch, and the verification
      # command pins the tag precisely so rehearsal bytes cannot pass as a
      # release. To repair a failed publish, dispatch with `--ref <tag>` so the
      # run is triggered by `refs/tags/<tag>` and provenance is correct by
      # construction. Re-pushing the tag does NOT work: it already points at the
      # same object, so git reports `Everything up-to-date` and no event fires.
      #
      # The test is on the ref itself, not on the event: `gh workflow run
      # release.yml --ref vX.Y.Z` is a legitimate manual repair whose
      # `github.ref` already IS the tag, and that must keep working.
      #
      # The mirror case matters just as much. A rehearsal dispatched with
      # `--ref vX.Y.Z` would mint provenance naming `refs/tags/vX.Y.Z`, which
      # the documented consumer check accepts — rehearsal bytes would verify as
      # a release. Rehearsals must therefore run from a branch.
      - name: Check that provenance will name the right ref
        env:
          MANUAL_RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          EXPECTED_REF="refs/tags/${RELEASE_TAG_OR_PLANNED}"

          if [ "$DRY_RUN" = "true" ]; then
            case "$GITHUB_REF" in
              refs/tags/*)
                echo "::error title=Rehearsal from a tag::A rehearsal dispatched from ${GITHUB_REF} would mint provenance naming that tag, so rehearsal bytes would satisfy the documented release verification. Rehearse from a branch instead." >&2
                exit 1
                ;;
            esac

            # A rehearsal must BUILD the ref it was dispatched from. With
            # `release_tag` set, `Checkout manual release tag` replaces the tree
            # with the tag's content while the certificate still records the
            # dispatching branch's commit — so `sourceRepositoryDigest` would
            # name a commit that did not produce these bytes, breaking the
            # exact-revision claim the verification instructions make.
            if [ -n "${MANUAL_RELEASE_TAG}" ]; then
              echo "::error title=Rehearsal of a different commit::This rehearsal builds refs/tags/${MANUAL_RELEASE_TAG} but provenance would name ${GITHUB_REF} at ${GITHUB_SHA}, which is not the commit that produced the artifacts. Rehearse without release_tag, from a branch pointing at the content you want to validate." >&2
              exit 1
            fi

            echo "Rehearsal provenance will name ${GITHUB_REF} at ${GITHUB_SHA}, which is also what it builds."
            exit 0
          fi

          if [ "$GITHUB_REF" != "$EXPECTED_REF" ]; then
            echo "::error title=Provenance ref mismatch::Sigstore records the ref that triggered the run, not the ref a later step checked out. This run was triggered by ${GITHUB_REF}, so the attestation would name that instead of ${EXPECTED_REF} and documented verification would reject the release. Re-run it as: gh workflow run release.yml --ref ${RELEASE_TAG_OR_PLANNED} -f release_tag=${RELEASE_TAG_OR_PLANNED} -f dry_run=false" >&2
            exit 1
          fi

          echo "Production provenance will name ${EXPECTED_REF}."

      - name: Verify release feed gate
        env:
          WINK_ALLOW_FIRST_RELEASE: ${{ vars.WINK_ALLOW_FIRST_RELEASE }}
        run: |
          MODE=release
          if [ "$DRY_RUN" = "true" ]; then MODE=rehearse; fi
          SPARKLE_PUBLIC_BASE_URL="$R2_PUBLIC_BASE_URL" bash scripts/verify-release-feed.sh --mode "$MODE"

      - name: Extract release notes
        run: |
          bash scripts/release-notes.sh "$VERSION" > "$RUNNER_TEMP/release-notes.md"
          if [ "$SIGNING_MODE" = "adhoc" ]; then
            {
              printf '\n---\n'
              printf 'First install on macOS 15+: System Settings → Privacy & Security → **Open Anyway** '
              printf '(this build is ad-hoc signed and not notarized — issue #283). '
              printf 'Updates after that arrive in-app via Sparkle.\n'
            } >> "$RUNNER_TEMP/release-notes.md"
          fi
          echo "RELEASE_NOTES_PATH=$RUNNER_TEMP/release-notes.md" >> "$GITHUB_ENV"

      - name: Import Developer ID certificate
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        env:
          DEVELOPER_ID_APP_CERT_BASE64: ${{ secrets.DEVELOPER_ID_APP_CERT_BASE64 }}
          DEVELOPER_ID_APP_CERT_PASSWORD: ${{ secrets.DEVELOPER_ID_APP_CERT_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          printf '%s' "$DEVELOPER_ID_APP_CERT_BASE64" | base64 -D > "$CERT_PATH"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$CERT_PATH" \
            -k "$KEYCHAIN_PATH" \
            -P "$DEVELOPER_ID_APP_CERT_PASSWORD" \
            -T /usr/bin/codesign \
            -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH"
          security default-keychain -d user -s "$KEYCHAIN_PATH"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

      - name: Write notarytool API key
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        env:
          NOTARYTOOL_KEY: ${{ secrets.NOTARYTOOL_KEY }}
        run: |
          printf '%s' "$NOTARYTOOL_KEY" > "$NOTARY_KEY_PATH"
          chmod 600 "$NOTARY_KEY_PATH"

      - name: Swift version
        run: swift --version

      - name: Test
        run: swift test

      - name: Package signed app bundle
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        env:
          SIGN_IDENTITY: ${{ secrets.DEVELOPER_ID_APP_SIGNING_IDENTITY }}
          ENTITLEMENTS_PLIST: ${{ github.workspace }}/entitlements.plist
          ENABLE_HARDENED_RUNTIME: "1"
          ENABLE_TIMESTAMP: "1"
          REQUIRE_SIGN_IDENTITY: "1"
          SPARKLE_PUBLIC_ED_KEY: ${{ secrets.SPARKLE_PUBLIC_ED_KEY }}
        run: bash scripts/package-app.sh

      - name: Package ad-hoc app bundle
        if: ${{ env.SIGNING_MODE == 'adhoc' }}
        env:
          SPARKLE_PUBLIC_ED_KEY: ${{ secrets.SPARKLE_PUBLIC_ED_KEY }}
        run: bash scripts/package-app.sh

      - name: Verify app signature
        run: |
          codesign --verify --deep --strict --verbose=2 build/Wink.app
          codesign --verify --strict --verbose=2 \
            build/Wink.app/Contents/Extensions/WinkFocusIntentsExtension.appex
          test -d build/Wink.app/Contents/Frameworks/Sparkle.framework
          if [ "$SIGNING_MODE" = "developer-id" ]; then
            codesign -d --entitlements :- build/Wink.app 2>&1 | grep -q 'group.com.wink.app'
            codesign -d --entitlements :- \
              build/Wink.app/Contents/Extensions/WinkFocusIntentsExtension.appex 2>&1 \
              | grep -q 'group.com.wink.app'
            codesign -d --entitlements :- \
              build/Wink.app/Contents/Extensions/WinkFocusIntentsExtension.appex 2>&1 \
              | grep -A1 -q 'com.apple.security.app-sandbox'
          fi

      - name: Assess app with Gatekeeper
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        run: spctl --assess --type exec --verbose build/Wink.app

      - name: Archive app bundle for notarization
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        run: |
          rm -f "$APP_NOTARIZATION_ZIP_PATH"
          ditto -c -k --sequesterRsrc --keepParent build/Wink.app "$APP_NOTARIZATION_ZIP_PATH"

      - name: Submit app notarization archive
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        env:
          NOTARYTOOL_KEY_ID: ${{ secrets.NOTARYTOOL_KEY_ID }}
          NOTARYTOOL_ISSUER: ${{ secrets.NOTARYTOOL_ISSUER }}
        run: |
          xcrun notarytool submit "$APP_NOTARIZATION_ZIP_PATH" \
            --key "$NOTARY_KEY_PATH" \
            --key-id "$NOTARYTOOL_KEY_ID" \
            --issuer "$NOTARYTOOL_ISSUER" \
            --wait

      - name: Staple app notarization ticket
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        run: xcrun stapler staple build/Wink.app

      - name: Validate notarized app bundle
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        run: |
          xcrun stapler validate build/Wink.app
          spctl --assess --type exec --verbose build/Wink.app

      - name: Package Sparkle update ZIP
        run: bash scripts/package-update-zip.sh

      - name: Verify Sparkle update ZIP
        run: test -f "$ZIP_PATH"

      - name: Generate appcast
        env:
          SPARKLE_PRIVATE_ED_KEY: ${{ secrets.SPARKLE_PRIVATE_ED_KEY }}
          WINK_ALLOW_FIRST_RELEASE: ${{ vars.WINK_ALLOW_FIRST_RELEASE }}
        run: |
          export SPARKLE_PUBLIC_BASE_URL="$R2_PUBLIC_BASE_URL"
          export SPARKLE_FULL_RELEASE_NOTES_URL="https://github.com/${GITHUB_REPOSITORY}/releases/tag/${RELEASE_TAG_OR_PLANNED}"
          bash scripts/generate-appcast.sh

      - name: Verify appcast
        run: |
          test -f "$APPCAST_PATH"
          grep -q "Wink-${VERSION}" "$APPCAST_PATH"
          grep -q 'edSignature' "$APPCAST_PATH"
          if [ -f build/live-appcast.xml ]; then
            OLD_COUNT="$(grep -c '<item>' build/live-appcast.xml || true)"
            NEW_COUNT="$(grep -c '<item>' "$APPCAST_PATH" || true)"
            if [ "$NEW_COUNT" -lt "$OLD_COUNT" ]; then
              echo "Error: generated appcast has $NEW_COUNT items but the live feed had $OLD_COUNT — history lost" >&2
              exit 1
            fi
          fi

      - name: Package DMG
        env:
          # Empty in adhoc mode — package-dmg.sh skips DMG signing when unset.
          DMG_SIGN_IDENTITY: ${{ secrets.DEVELOPER_ID_APP_SIGNING_IDENTITY }}
        run: bash scripts/package-dmg.sh

      - name: Submit DMG for notarization
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        env:
          NOTARYTOOL_KEY_ID: ${{ secrets.NOTARYTOOL_KEY_ID }}
          NOTARYTOOL_ISSUER: ${{ secrets.NOTARYTOOL_ISSUER }}
        run: |
          xcrun notarytool submit "$DMG_PATH" \
            --key "$NOTARY_KEY_PATH" \
            --key-id "$NOTARYTOOL_KEY_ID" \
            --issuer "$NOTARYTOOL_ISSUER" \
            --wait

      - name: Staple notarization ticket
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        run: xcrun stapler staple "$DMG_PATH"

      - name: Validate final DMG
        if: ${{ env.SIGNING_MODE == 'developer-id' }}
        run: |
          xcrun stapler validate "$DMG_PATH"
          spctl --assess --type open --context context:primary-signature --verbose "$DMG_PATH"

      - name: Verify ad-hoc DMG exists
        if: ${{ env.SIGNING_MODE == 'adhoc' }}
        run: test -f "$DMG_PATH"

      # Attestation placement is load-bearing (issue #443):
      #
      #   AFTER stapling — `xcrun stapler staple` writes the notarization ticket
      #   into the artifact, changing its bytes and therefore its SHA-256. An
      #   attestation created before stapling names a digest no downloader will
      #   ever see.
      #
      #   BEFORE any upload — R2, the GitHub Release, and the appcast must only
      #   ever receive bytes that are already attested, so a failure here stops
      #   the release instead of leaving unattested artifacts published.
      #
      # Both DMG and Sparkle ZIP are final at this point: the ZIP is built from
      # the app bundle after that bundle was stapled, and nothing repackages
      # either file below.
      #
      # `appcast.xml` is deliberately NOT attested. It is a mutable feed that is
      # rewritten on every release and keeps Sparkle's EdDSA signature model;
      # an immutable digest attestation would be false the moment it is updated.
      - name: Record final artifact digests
        run: |
          shasum -a 256 "$DMG_PATH" "$ZIP_PATH" | tee "$RUNNER_TEMP/release-digests.txt"
          {
            echo "## Attested artifacts"
            echo
            echo '```'
            cat "$RUNNER_TEMP/release-digests.txt"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Attest final DMG and Sparkle ZIP
        id: attest
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: |
            ${{ env.DMG_PATH }}
            ${{ env.ZIP_PATH }}

      # A rehearsal signs real bytes and produces a real attestation — there is
      # no "unpublished" attestation mode. What separates it from production
      # provenance is the source ref recorded in the certificate: a rehearsal
      # carries `refs/heads/<branch>`, a production release carries
      # `refs/tags/vX.Y.Z`. The consumer command in VERIFYING_RELEASES.md pins
      # `--source-ref` for exactly that reason.
      - name: Summarize attestation provenance mode
        env:
          ATTESTATION_URL: ${{ steps.attest.outputs.attestation-url }}
        run: |
          if [ "$DRY_RUN" = "true" ]; then
            echo "::notice title=Rehearsal provenance::Attestation created for rehearsal bytes at ${GITHUB_REF}. It is NOT production provenance: verification pinned to refs/tags/${RELEASE_TAG_OR_PLANNED} will reject it."
          else
            echo "::notice title=Production provenance::Attestation created for ${RELEASE_TAG} artifacts."
          fi
          echo "Attestation: ${ATTESTATION_URL:-(none)}" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload dry-run artifacts
        if: ${{ env.DRY_RUN == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-dry-run-${{ env.VERSION }}
          path: |
            build/*.dmg
            build/*.zip
            build/appcast.xml

      - name: Install boto3 in a virtual environment
        if: ${{ env.DRY_RUN != 'true' }}
        run: |
          python3 -m venv "$RUNNER_TEMP/r2-upload-venv"
          "$RUNNER_TEMP/r2-upload-venv/bin/pip" install boto3
          echo "R2_UPLOAD_PYTHON=$RUNNER_TEMP/r2-upload-venv/bin/python" >> "$GITHUB_ENV"

      - name: Upload DMG and Sparkle ZIP to R2
        if: ${{ env.DRY_RUN != 'true' }}
        env:
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          "$R2_UPLOAD_PYTHON" scripts/upload-r2-object.py "$ZIP_PATH" application/zip
          "$R2_UPLOAD_PYTHON" scripts/upload-r2-object.py "$DMG_PATH" application/x-apple-diskimage

      - name: Publish GitHub Release
        if: ${{ env.DRY_RUN != 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" "$DMG_PATH" --clobber
            gh release edit "$RELEASE_TAG" --notes-file "$RELEASE_NOTES_PATH"
          else
            gh release create "$RELEASE_TAG" "$DMG_PATH" \
              --title "$RELEASE_TAG" \
              --notes-file "$RELEASE_NOTES_PATH"
          fi

      - name: Publish live Sparkle appcast to R2
        if: ${{ env.DRY_RUN != 'true' }}
        env:
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          "$R2_UPLOAD_PYTHON" scripts/upload-r2-object.py "$APPCAST_PATH" application/xml

      - name: Cleanup temporary credentials
        if: always()
        run: |
          rm -f "$CERT_PATH" "$NOTARY_KEY_PATH"
          security delete-keychain "$KEYCHAIN_PATH" || true
