name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build and Test
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Governance checks run before the toolchain work: they cost seconds and
      # they are the gate that keeps every `uses:` reference immutable (#439).
      - name: Validate immutable action references
        run: node .github/scripts/validate-workflow-pins.mjs

      # Deliberately shell-expanded rather than a Node glob or a bare directory:
      # `node --test <dir>` is a module path on Node 22+, and quoted-glob support
      # is version-dependent. An explicit file list works on every Node the
      # runner image has ever shipped, and a zero-match glob fails loudly.
      - name: Governance script tests
        run: node --test .github/scripts/tests/*.test.mjs

      - name: Swift version
        run: swift --version

      - name: Capture toolchain cache key
        run: echo "TOOLCHAIN_KEY=$(swift --version 2>/dev/null | head -1 | shasum -a 256 | cut -c1-12)" >> "$GITHUB_ENV"

      - name: Cache SwiftPM build
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .build
          key: spm-${{ runner.os }}-${{ env.TOOLCHAIN_KEY }}-${{ hashFiles('Package.resolved') }}
          restore-keys: spm-${{ runner.os }}-${{ env.TOOLCHAIN_KEY }}-

      - name: Release script unit tests
        run: |
          brew install bats-core
          bats scripts/verify-release-feed.bats scripts/release-notes.bats scripts/bump-version.bats

      - name: Verify localization catalog is compiled
        run: bash scripts/gen-localizations.sh --check

      - name: Build (debug)
        run: swift build

      - name: Test
        run: swift test

      - name: Build (release)
        run: swift build -c release

      - name: Generate temporary Sparkle signing keys
        run: |
          SPARKLE_KEY_OUTPUT_DIR="$RUNNER_TEMP/sparkle-ci-keys" \
            bash scripts/export-test-sparkle-keys.sh >> "$GITHUB_ENV"
          echo "SPARKLE_PUBLIC_BASE_URL=https://example.invalid/wink/" >> "$GITHUB_ENV"
          echo "SPARKLE_FEED_URL=https://example.invalid/wink/appcast.xml" >> "$GITHUB_ENV"
          echo "MACOS_MINIMUM_SYSTEM_VERSION=15.0" >> "$GITHUB_ENV"

      - name: Package app bundle
        run: bash scripts/package-app.sh

      - name: Verify app bundle
        run: |
          test -f build/Wink.app/Contents/MacOS/Wink
          test -f build/Wink.app/Contents/Info.plist
          test -d build/Wink.app/Contents/Frameworks/Sparkle.framework
          test "$(/usr/libexec/PlistBuddy -c 'Print :LSMinimumSystemVersion' build/Wink.app/Contents/Info.plist)" = "$MACOS_MINIMUM_SYSTEM_VERSION"
          test "$(/usr/libexec/PlistBuddy -c 'Print :SUFeedURL' build/Wink.app/Contents/Info.plist)" = "$SPARKLE_FEED_URL"
          test "$(/usr/libexec/PlistBuddy -c 'Print :SUPublicEDKey' build/Wink.app/Contents/Info.plist)" = "$SPARKLE_PUBLIC_ED_KEY"
          file build/Wink.app/Contents/MacOS/Wink | grep -q "Mach-O"

      - name: Package Sparkle update ZIP
        run: bash scripts/package-update-zip.sh

      - name: Verify Sparkle update ZIP
        run: |
          VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' Sources/Wink/Resources/Info.plist)"
          test -f "build/Wink-${VERSION}.zip"

      - name: Generate Sparkle appcast
        run: SPARKLE_RESTORED_APPCAST=scripts/fixtures/ci-old-appcast.xml bash scripts/generate-appcast.sh

      - name: Verify Sparkle appcast
        run: |
          test -f build/appcast.xml
          grep -q "Wink-" build/appcast.xml
          grep -q "https://example.invalid/wink/" build/appcast.xml
          # Merge-semantics rehearsal assertions (spec §2): the fixture entry must survive
          # untouched next to the freshly generated entry.
          grep -q "https://old.example.invalid/Wink-0.0.1.zip" build/appcast.xml
          grep -q "fixturesignature==" build/appcast.xml
          grep -q "<sparkle:version>1</sparkle:version>" build/appcast.xml
          test "$(grep -c '<item>' build/appcast.xml)" -ge 2

      - name: Package DMG
        run: bash scripts/package-dmg.sh

      - name: Verify DMG
        run: |
          VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' Sources/Wink/Resources/Info.plist)"
          test -f "build/Wink-${VERSION}.dmg"

      - name: Verify R2 upload helper (dry run)
        run: |
          VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' Sources/Wink/Resources/Info.plist)"
          export R2_ACCOUNT_ID=test-account
          export R2_ACCESS_KEY_ID=test-access-key
          export R2_SECRET_ACCESS_KEY=test-secret-key
          export R2_BUCKET=test-bucket
          export R2_PUBLIC_BASE_URL="$SPARKLE_PUBLIC_BASE_URL"
          python3 scripts/upload-r2-object.py "build/Wink-${VERSION}.zip" application/zip --dry-run
          python3 scripts/upload-r2-object.py build/appcast.xml application/xml --dry-run
          python3 scripts/upload-r2-object.py "build/Wink-${VERSION}.dmg" application/x-apple-diskimage --dry-run

      - name: Cleanup temporary Sparkle signing key
        if: always()
        run: |
          if [ -n "${SPARKLE_KEY_ACCOUNT:-}" ]; then
            security delete-generic-password -s https://sparkle-project.org -a "$SPARKLE_KEY_ACCOUNT" >/dev/null 2>&1 || true
          fi
          rm -rf "$RUNNER_TEMP/sparkle-ci-keys"
